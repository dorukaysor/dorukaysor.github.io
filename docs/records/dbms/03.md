---
title: Experiment 03
sidebar_label: EXP 03
---

### **Aim**
> To study and implement various data types in SQL.

### **Theory**

**Different datatypes:**
- **Numeric Data Types:**
  - `INT`: Integer values
  - `DECIMAL(p, s)`: Fixed-point numbers with precision `p` and scale `s`
  - `FLOAT`: Floating-point numbers

- **String Data Types:**
  - `CHAR(n)`: Fixed-length string of length `n`
  - `VARCHAR(n)`: Variable-length string with a maximum length of `n`
  - `TEXT`: Large text data

- **Date and Time Data Types:**
  - `DATE`: Date values (YYYY-MM-DD)
    - `TIMESTAMP`: Date and time values (YYYY-MM-DD HH:MM:SS)
    - `DATETIME`: Date and time values (YYYY-MM-DD HH:MM:SS)
  - `TIME`: Time values (HH:MM:SS)
  - `YEAR`: Year values (YYYY)


### **Example Script**
```sql title='Implementing some datatypes in SQL'
CREATE TABLE ProductInventory (
    ProductID INT PRIMARY KEY,
    Price DECIMAL(10, 2),
    Weight FLOAT,
    ProductCode CHAR(5),
    ProductName VARCHAR(100),
    Description TEXT,
    MfgDate DATE,
    LastUpdated TIMESTAMP
);
DESCRIBE ProductInventory;
```

<details>
  <summary><b>Table</b></summary>

  Draw the table that the following script will give as output
  ```sql
    DESCRIBE ProductInventory;
  ```

</details>

### **Conclusion**
> In this experiment, we successfully studied and implemented various SQL data types